# Block any payload containing "${jndi:" (case-insensitive)
SecRule ARGS|REQUEST_HEADERS "@rx (?i)\$\{\s*jndi\s*:" \
    "id:900001,phase:1,deny,log,msg:'Blocked potential Log4Shell attempt: %{MATCHED_VAR}'"
